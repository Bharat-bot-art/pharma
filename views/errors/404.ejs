<%
  const page = '';
%>
<%- include('../partials/head', { page, title }) %>
<div class="container" style="padding:80px 0;text-align:center">
  <div class="empty-state">
    <div class="art" style="width:150px;height:150px;background:var(--brand-gradient-soft);border-radius:50%;display:grid;place-items:center;font-family:var(--font-display);font-size:44px;font-weight:800;color:var(--green-600)">404</div>
    <h1 style="font-size:26px;margin-top:18px">Page not found</h1>
    <p class="text-muted">The page you're looking for doesn't exist or has moved.</p>
    <div class="row" style="justify-content:center;margin-top:20px">
      <a class="btn btn-primary" href="/">Go Home</a>
      <a class="btn btn-outline" href="/shop">Browse Medicines</a>
    </div>
  </div>
</div>
<%- include('../partials/footer') %>
